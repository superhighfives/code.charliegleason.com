import type { UseScrambleProps } from "use-scramble";

export const scrambleOptions: UseScrambleProps = {
  speed: 0.35,
  tick: 1,
  step: 1,
  scramble: 4,
  overdrive: false,
  playOnMount: false,
  overflow: true,
  range: [65, 90],
};

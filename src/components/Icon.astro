---
// Icon component - placeholder for MDX compatibility
// The original site uses lucide-react icons like HandMetal
// For now, this renders nothing as the original also seems to skip it
interface Props {
  name?: string;
  className?: string;
  size?: string | number;
}

const { name, className, size } = Astro.props;
---

{/* Icon component - silently ignored to match original behavior */}
